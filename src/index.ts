const foo=1
const bar = [1,2,3];
function   baz()  { console.log('Hello') }

if(true){
console.log('Indented block');
}

const obj ={ prop: 'value' };

// const longFunctionCall = someFunction(arg1,arg2,arg3,arg4);

const multilineString = 'This is a multiline string with a long line that exceeds the Prettier print width. This line should be automatically wrapped to fit the specified print width in ESLint and Prettier configurations.';

console.log('Testing ESLint and Prettier configuration');
